import joblib
from sklearn.metrics import accuracy_score, classification_report, confusion_matrix, ConfusionMatrixDisplay

# Loading the saved model
model_2_loaded = joblib.load("model_2.pkl")

# Test predictions using the loaded model
y_test_pred_loaded = model_2_loaded.predict(x_test_flat)

# Test accuracy for the loaded model
test_accuracy_loaded = accuracy_score(y_test, y_test_pred_loaded)
print("Test Accuracy for the loaded model:", test_accuracy_loaded)

# Classification report for the loaded model
print("Classification Report for the loaded model:")
print(classification_report(y_test, y_test_pred_loaded))

# Confusion matrix for the loaded model
conf_matrix_loaded = confusion_matrix(y_test, y_test_pred_loaded)
print("Confusion Matrix for the loaded model:")
print(conf_matrix_loaded)

# Display the confusion matrix
ConfusionMatrixDisplay(conf_matrix_loaded).plot()