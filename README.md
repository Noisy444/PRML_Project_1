# PRML_Project_1
Simple logistic regression project using the fashion MNIST dataset.

Must pip install related libraries.

Data validation is an exploration of the fashion MNIST dataset

Model 1 is a logistic regression model of a C value of 1

Model compare, compares model 1 to a model which has a C value of 0.1